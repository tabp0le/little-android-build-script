repo sync && . build/envsetup.sh && source config.conf && breakfast $DEVICECODENAME && make installclean && brunch $DEVICECODENAME && cd $OUT && export FILENAME=$(ls R*.zip) && export MD5SUMNAME=$(ls R*.md5sum) && . build/upload-sftp $FTPUSER@$FTPSERVER:$FTPPATH $FILENAME $MD5SUMNAME -oIdentityFile=$FTPSSHKEY && cd $OTAPATH && awk '{gsub(/RR-N-v5.8.2-*-pme-Official.zip/,"$FILENAME")}' pme.xml && git commit . -S -m $OTACOMMITMSG && git push $OTAREPO
